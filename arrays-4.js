var ProgrammingLanguages = ['python', 'java', 'c', 'pascal', 'c++', 'javascript', 'kotlin']

for(let i = 0; i < ProgrammingLanguages.length; i++){
    if(ProgrammingLanguages[i].length > 3){
        console.log(ProgrammingLanguages[i]);
    }
}